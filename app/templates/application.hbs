{{nav-bar onSignIn=(action "signIn") onSignOut=(action "signOut") }}

<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <div class="error-message">
        {{#if errorMessage}}
          <p>{{errorMessage}}</p>
        {{/if}}
      </div>
      {{outlet}}
    </div>
  </div>
</div>
